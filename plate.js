import { initializeApp } from "https://www.gstatic.com/firebasejs/12.17.0/firebase-app.js";
import { doc, getFirestore, serverTimestamp, setDoc } from "https://www.gstatic.com/firebasejs/12.17.0/firebase-firestore.js";
const firebaseConfig=window.LUCKY_GARAGE_FIREBASE_CONFIG||{}, config=window.YU_STORE_CONFIG||{}; const db=getFirestore(initializeApp(firebaseConfig)); const $=(s)=>document.querySelector(s);
const money=(v)=>`NT$${Math.max(0,Number(v)||0).toLocaleString("zh-TW")}`; let captcha=0;
function cleanPhone(v){let d=String(v||"").replace(/\D/g,"");if(d.startsWith("886"))d=`0${d.slice(3)}`;return d;} function makeId(){const d=new Date(),ymd=`${d.getFullYear()}${String(d.getMonth()+1).padStart(2,"0")}${String(d.getDate()).padStart(2,"0")}`,chars="ABCDEFGHJKLMNPQRSTUVWXYZ23456789",bytes=new Uint32Array(4);crypto.getRandomValues(bytes);return `PLATE-${ymd}-${[...bytes].map(v=>chars[v%chars.length]).join("")}`;} function toast(m){const t=$("#toast");t.textContent=m;t.classList.add("show");clearTimeout(toast.timer);toast.timer=setTimeout(()=>t.classList.remove("show"),3200);} function newCaptcha(){const a=Math.floor(Math.random()*8)+2,b=Math.floor(Math.random()*8)+2;captcha=a+b;$("#captchaQuestion").textContent=`人類驗證：${a} + ${b} = ?`;$("#captchaAnswer").value="";}
const price=Number(config.platePrice||9500),deposit=Number(config.plateDeposit||5500),balance=Number(config.plateBalance||4000);$("#priceText").textContent=money(price);$("#depositText").textContent=money(deposit);$("#balanceText").textContent=money(balance);
$("#plateNumber").addEventListener("input",()=>{const v=$("#plateNumber").value.toUpperCase().replace(/[^A-Z0-9-]/g,"").slice(0,12);$("#plateNumber").value=v;$("#platePreview").textContent=v||"ABC-1234";});
$("#plateForm").addEventListener("submit",async(e)=>{e.preventDefault();if($("#website").value)return;if(Number($("#captchaAnswer").value)!==captcha){newCaptcha();toast("驗證答案錯誤，請重新計算。");return;}const customerName=$("#name").value.trim(),phone=cleanPhone($("#phone").value),address=$("#address").value.trim(),plateNumber=$("#plateNumber").value.trim().toUpperCase(),plateType=$("#plateType").value;if(!customerName||phone.length<9||address.length<3||!plateNumber){toast("請完整填寫訂製資料。");return;}const orderId=makeId(),data={orderNo:orderId,orderId,source:"official-store-plate",customerName,custName:customerName,phone,custPhone:phone,address,custAddress:address,model:"紀念展示車牌",itemName:`${plateType}｜${plateNumber}`,color:"依訂製樣式",vehicleVariant:plateType,battery:"不適用",price,totalAmount:money(price),cost:0,netProfit:price,deposit:0,balancePaid:0,licenseMode:"自行辦理",deliveryMode:"物流寄送",paymentMethod:"轉帳",paymentTerms:`訂金 ${money(deposit)}／尾款 ${money(balance)}（貨到付款）`,status:"待訂金",deliveredAt:"",notes:`訂製號碼：${plateNumber}。僅供紀念展示，不可上路使用。`,createdAt:serverTimestamp(),updatedAt:serverTimestamp(),timestamp:serverTimestamp(),createdBy:"public-store",updatedBy:"public-store"};const btn=$("#submitBtn");btn.disabled=true;btn.textContent="送出中…";try{await setDoc(doc(db,"orders",orderId),data);const msg=`您好小宇，我要訂製紀念展示牌：\n訂單編號：${orderId}\n類型：${plateType}\n號碼：${plateNumber}\n姓名：${customerName}\n電話：${phone}\n收件資料：${address}\n付款方式：訂金 ${money(deposit)}／尾款 ${money(balance)}（貨到付款）\n請協助確認付款。`;try{await navigator.clipboard.writeText(msg);}catch{}toast(`訂製需求 ${orderId} 已建立，正在開啟 LINE。`);setTimeout(()=>window.location.href=(config.lineUrl||"https://line.me/R/ti/p/@762eqvlg"),650);e.target.reset();$("#plateNumber").value="ABC-1234";$("#platePreview").textContent="ABC-1234";newCaptcha();}catch(err){console.error(err);toast("送出失敗，請直接聯絡官方 LINE。");}finally{btn.disabled=false;btn.textContent="送出訂製需求並開啟 LINE";}});newCaptcha();
